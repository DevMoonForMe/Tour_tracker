# Tour Split Tracker Environment Configuration

# Custom path to JSON database directory (defaults to ./data if unset)
DATA_DIR="./data"

# Optional Next.js public app title
NEXT_PUBLIC_APP_TITLE="Tour Split Tracker"
